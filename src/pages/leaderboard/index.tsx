import { PageTitle } from "@/shared";
import { Leaderboard } from "@/widgets/leaderboard";

export const LeaderboardPage = () => {
  return (
    <>
      <PageTitle>Лидерборды</PageTitle>
      <Leaderboard />
    </>
  );
};

export default LeaderboardPage;
